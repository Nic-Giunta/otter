# Benchmarks

Benchmarks use only the public Otter API. They are intentionally simple so contributors can run them without optional dependencies.

Run a benchmark directly:

```bash
python benchmarks/bench_core.py
```

For credible performance claims, include Python version, operating system, hardware, dataset shape, dtype mix, null rate, and Otter version.
